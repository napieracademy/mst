// Configurazione personalizzata per Next.js
// Questo file viene importato da next.config.mjs

/** @type {import('next').NextConfig} */
const userConfig = {
  // La configurazione qui sovrascriverà quella in next.config.mjs
}

export default userConfig