import { useEffect, useState } from 'react';
import { fetchImdbAwards } from '@/utils/imdb-api';

// Questo componente è stato completamente disabilitato.
// La funzione fetchImdbAwards può essere utilizzata per ottenere i dati, ma il componente è disattivato.

export function MovieAwards({ imdbId }: { imdbId: string }) {
  // Non facciamo più nulla, il componente è totalmente disabilitato
  return null;
}
